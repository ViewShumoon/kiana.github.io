
VITE_PORT = 9000
VITE_DEV_DEFAULT_ROUTE = "/JapaneseGojuon"
# 登录服务地址：默认连线上 Worker（已放行 http://localhost:9000 跨域）
# 需要改连本地 Worker（npx wrangler dev --config src/worker/wrangler.toml）时，
# 新建不入库的 .env.development.local 覆盖为 http://localhost:8787 即可
VITE_AUTH_API_URL = "https://auth.shumoon.cn"
